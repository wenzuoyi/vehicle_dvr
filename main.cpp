#include <time.h>
#include <string.h>
#include <cstdio>

#include <iostream>
#include <fstream>
#include <string>

#include  "CSerialPort.h"
#include "CLog.h"

int main()
{
//    CLog::getInstance(std::string("./vehicle_dvr.log")).write("Hello, the world!", CLog::Info);
 //   CLog::getInstance(std::string("./vehicle_dvr.log")).write("Hello, wenzuoyi", CLog::Info);
  //  CLog::getInstance(std::string("./vehicle_dvr.log")).write("Hello, zhiCloud!", CLog::Info);

    std::cout<<"Hello, the world!"<<std::endl;
    return 0;
}
